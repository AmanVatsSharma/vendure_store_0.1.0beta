/* @Web - Deprecated duplicate. Use LogoProMerchants instead. */
import React from 'react';
import { LogoProMerchants } from './LogoProMerchants';

export const LogoProMerchantsDark: React.FC<{ width?: number; height?: number }> = (props) => {
  return <LogoProMerchants {...props} />;
};
